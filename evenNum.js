// should take a number as input and return true 
// if its an even number false for odd numbers

function isEven(number) {
  if (number % 2 === 0) { // 1
    return true // 1
  } else { // 1
    return false // 1
  }
}

//O(1) constant time
